---
import BaseLayout from '@/layouts/BaseLayout.astro';

const title = 'SEO 学习笔记:本站用到的优化技巧记录 | 明日方舟图鉴';
const description =
  '记录本站实践的 SEO 优化技巧:title 标题、meta description、H1/H2 层级、图片 alt、内链锚文本与结构化数据,边做边学。';
---

<BaseLayout title={title} description={description}>
  <h1>SEO 学习笔记</h1>
  <p class="lead">本站每个页面都实践了下面这些 SEO 要点,可以「查看网页源代码」对照学习。</p>

  <div class="note">
    <h2 style="margin-top:0">① title 与 meta description</h2>
    <p>搜索结果里那条蓝色标题来自 <code>&lt;title&gt;</code>,灰色描述来自
      <code>&lt;meta name="description"&gt;</code>。每页都应独一无二、含核心关键词。</p>
  </div>

  <div class="note">
    <h2 style="margin-top:0">② canonical 规范链接</h2>
    <p><code>&lt;link rel="canonical"&gt;</code> 告诉搜索引擎页面的权威地址,避免
      <code>?from=xxx</code> 等参数造成的重复内容稀释权重。</p>
  </div>

  <div class="note">
    <h2 style="margin-top:0">③ 结构化数据 JSON-LD</h2>
    <p>用 <code>schema.org</code> 词汇(如 <code>Person</code>、<code>CollectionPage</code>)
      以机器可读方式描述内容,有机会获得富媒体搜索结果。打开人物页源码看
      <code>application/ld+json</code>。</p>
  </div>

  <div class="note">
    <h2 style="margin-top:0">④ 语义化 HTML</h2>
    <p>使用 <code>header / nav / main / article / footer</code> 和层级化的
      <code>h1~h3</code>,让爬虫准确理解页面结构与主题。</p>
  </div>

  <div class="note">
    <h2 style="margin-top:0">⑤ sitemap.xml 与 robots.txt</h2>
    <p>构建时由 <code>@astrojs/sitemap</code> 自动生成 <code>/sitemap-index.xml</code>,
      列出所有页面方便收录;<code>/robots.txt</code> 指明抓取规则并指向 sitemap。</p>
  </div>

  <div class="note">
    <h2 style="margin-top:0">⑥ 渐进增强 / 可抓取内容</h2>
    <p>关系图用 canvas 渲染,爬虫读不到。所以首页同时提供「文字版人物名录」,
      并让每个角色拥有独立的静态页面 —— 交互体验与可索引内容兼得。</p>
  </div>
</BaseLayout>
